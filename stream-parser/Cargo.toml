[package]
name = "agent-stream"
version = "0.1.0"
edition = "2021"
description = "Stream parser for Agent Orchestra - normalizes agent output to unified events"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
